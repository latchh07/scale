{
  "name": "team12-meridian-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Meridian - Financial Crime Operations workbench for TrustSphere Bank (SCALE 2026, Team 12)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "esbuild ./ssr.test.jsx --bundle --platform=node --format=cjs --loader:.jsx=jsx --outfile=node_modules/.cache/ssr.cjs --log-level=error && node node_modules/.cache/ssr.cjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "esbuild": "^0.24.0",
    "vite": "^5.4.11"
  }
}
